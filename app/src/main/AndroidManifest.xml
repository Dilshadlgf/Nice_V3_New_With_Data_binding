<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.testproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestProject">
        <activity
            android:name=".ui.Activity.FarmerRegistrationActivity"
            android:exported="false" />
            <activity
            android:name=".ui.Fragment.Farmer.ContentListFragment"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <activity
            android:theme="@style/Theme.TestProject"
            android:name=".ui.Activity.FarmerMainActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".ui.Activity.FarmerLoginActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".ui.Activity.MainSplashActivity"
            android:theme="@style/Theme.Fab_Bottom_app_bar"
             android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>